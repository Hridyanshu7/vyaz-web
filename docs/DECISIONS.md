# Vyaz — Decision Log (ADRs)

_Append-only record of the significant decisions and pivots, with the **why** behind them. Newest sections grouped by theme. Format per entry: **Decision · Status · Why · Alternatives considered.** Last updated: 2026-07-09 (A15/A16)._

> **How to use this:** this is the "don't lose the why" record. When a decision changes, add a new entry that supersedes the old one (don't silently edit history). Companion docs: [`ARCHITECTURE.md`](ARCHITECTURE.md) (current system), [`pricing.md`](pricing.md), [`unit-economics.md`](unit-economics.md), [`voice-providers-comparison.md`](voice-providers-comparison.md).

---

## A. Voice / AI narration (the core product arc)

### A1. Voice provider evolution: Cartesia Line → custom pipeline → Gemini Live
- **Status:** Active — **Gemini Live** is the primary. **Superseded 2026-07-12 — see C7:** Cartesia and the custom pipeline are no longer selectable at all; their modals are deleted.
- **Why:** Started on **Cartesia Line** (managed voice agent, code-based, Claude Haiku) as the benchmark — great latency (Sonic TTS 40–90ms) but **ran out of credits**. Built a **custom STT→LLM→TTS pipeline** on Gemini as a fallback, but chaining three sequential API calls gave **high latency** and the browser-TTS fallback sounded robotic. Moved to **Gemini Live** (full-duplex WebSocket, one connection does STT+LLM+TTS) — cheapest all-in (~$0.02–0.03/min), low latency (~320ms), natively multilingual (incl. ~10 Indian languages), and already on our key.
- **Alternatives:** Sarvam, Smallest.ai (parked — API keys / sales-gated); ElevenLabs, Vapi, Retell, LiveKit, Pipecat, OpenAI Realtime (see [voice-providers-comparison.md](voice-providers-comparison.md)).

### A2. Provider switcher = one global combo
- **Status:** **Superseded 2026-07-12 — see C7.** There's no switcher anymore — `BookDetail` renders `GeminiLiveModal` unconditionally, no `platform_settings.voice_provider` branching.
- **Why (historical):** simpler than per-book/per-user config; let us A/B providers platform-wide while more than one existed.

### A3. Gemini Live model + Developer API vs Vertex
- **Status:** Active — default **`gemini-3.1-flash-live-preview`**.
- **Why:** `gemini-2.0-flash-live-001` was **shut down**; `gemini-2.5-flash-native-audio-preview` is more natural but **flaky** (mid-turn drops). `gemini-live-2.5-flash-native-audio` is a **Vertex-only** ID — the Developer API rejects it (this cost us a debugging cycle). **Key constraint:** Developer API allows **~3 concurrent sessions/key**; **Vertex AI = 1,000/project** — Vertex is the path to scale. Preview models change server-side without notice (bit us once — see A9).

### A4. Verbatim narration, NOT discussion mode
- **Status:** Active (verbatim). **This reversed an earlier decision** — important.
- **History:** First built free-form narration → the model **paraphrased/summarized**, which broke progress tracking + classification. Tried **discussion mode** (orchestrated section-by-section) → but discussion fundamentally can't support verbatim progress/classification, and it compromised Q&A context. **Reverted to verbatim**: the agent reads the section text word-for-word.
- **Why:** Progress tracking and black/grey classification both **require verbatim delivery** to work. "Discussion tonality" + "accurate tracking" are in direct tension; verbatim won.
- **Trade-off accepted:** longer sessions + higher audio-output cost vs. summaries.

### A5. `((...))` prompt-driven aside marking (classification)
- **Status:** Active.
- **Decision:** The system prompt instructs the model to wrap **everything that is not the book's exact words** (its own remarks, questions, check-ins, answers) in `((double parentheses))`. Client parses: outside parens = **black** (book verbatim), inside = **grey** (agent's own). Parens are stripped for display.
- **Why:** Earlier we classified by **verbatim text-matching** against the chapter — worked only while the model read verbatim, and broke the moment it paraphrased. Then tried **role-based** (turn context). Settled on prompt-emitted markers because it's the only reliable signal when the model paraphrases, and it moves control to the prompt. **Caveat:** double-parens must stay *silent* in TTS (item #3 in the action plan tracks a fallback marker if they get vocalized).

### A6. Progress + section tracking = one verbatim word-pointer
- **Status:** Active.
- **Decision:** A single pointer advances by **word-aligning the verbatim (non-aside) narration** against the chapter text. It drives **both** the % bar and the section states (done/active/remaining). Asides and Q&A never advance it.
- **Why:** One source of truth; only actual book-reading moves progress. Directly depends on A4 (verbatim) + A5 (parens exclude asides).

### A7. RAG / retrieval — parked
- **Status:** Parked ("next likely work").
- **Decision:** Today the **full current chapter** is stuffed into the Live system prompt; the agent answers only from that chapter (no whole-book reach). Chose to keep chapter-only for now.
- **Why deferred:** Full-book Q&A needs retrieval (embeddings + a `lookup()` function-call tool). It's the right fix for cost (Live re-bills context) + cross-chapter Q&A, but not needed yet. Naive section-by-section feeding was rejected (breaks Q&A context).

### A8. Voice interaction = voice-only; generation params planned
- **Status:** Active (voice-only: speak to interrupt/continue; only scroll + End Session are buttons). Generation params (temp 0.2, top_p 0.8, max-tokens) are **planned** (action items 9–14).
- **Why low temp:** verbatim faithfulness — low temperature/top-P reduce paraphrasing. **Explicitly avoid** frequency/presence penalties (they'd penalize the book's natural repetition → push the model to alter the text).

### A9. Voice glitches = main-thread congestion, NOT echo
- **Status:** Diagnosed; fix **parked** (reload workaround for now).
- **Why it matters:** Symptoms (choppy audio + laggy text after a few turns) first looked like acoustic echo / upstream VAD change. **Git proved no voice code changed that day**, and a **hard-reload fixed it** → root cause is **main-thread congestion**: mic capture uses a deprecated `ScriptProcessorNode` (main thread) competing with the 60fps waveform + React re-renders. **Fix ladder (parked):** lighten main-thread load (throttle waveform, optimize per-chunk transcript work) first; migrate mic capture to an **AudioWorklet** only if needed. Lesson: "it worked yesterday, no code change" → suspect environment/upstream/perf, and verify with git + a reload test before theorizing.

### A10. Transcript bubble fidelity: render tail-drop + cross-turn buffer resets
- **Status:** Fixed (2026-07-06).
- **Symptom:** words the agent *spoke in audio* were missing from the on-screen bubble — mid/end of a segment and, in a later regression, the *beginning* of a turn; user bubbles clipped too.
- **Causes & fixes:** (1) **Render tail-drop** — `toParagraphs` (`GeminiLiveModal.jsx`) split narration on `.!?` and **discarded any not-yet-terminated trailing text**, so the tail of every streaming/segment-ending run was thrown away. Fixed to preserve the remainder after the last matched sentence. (2) **Cross-turn buffer lifecycle** (`geminiLive.js`) — the agent bubble now resets on `turnComplete`/`interrupted`, and the **user** bubble resets when the *model begins replying* (`_startAgentBubble`), **not** on the model's `turnComplete` (which can land *after* the user already started their next utterance, wiping its opening words). A bubble is created by whichever signal (audio or transcription) arrives first.
- **Rejected:** a "lazy reset / audio-anchored rotation" attempt — it glued the next turn's opening words onto the previous bubble (beginning-eating). Reverted to eager reset.
- **Principle reaffirmed:** downstream *render/buffer* loss (text the model DID produce) is a code fix — distinct from prompt-governed behaviour (paraphrasing, missed `((...))` → `live_system_prompt`) and from pre-model capture/VAD loss.

### A11. Long-chapter session resumption + auto-reconnect
- **Status:** Shipped (2026-07-06) — **supersedes** the "session resumption not built" note in A9 / D3.
- **Decision:** setup enables `sessionResumption` + `contextWindowCompression: { slidingWindow }`; the client stores the server's `sessionResumptionUpdate` handle and, on an unexpected `ws.onclose` (not an intentional `end()`), transparently re-opens with the handle (up to 5 attempts, backoff), **reusing the live mic/AudioContexts + in-memory progress pointer**, then nudges the model to continue verbatim. A `this.ws !== ws` guard ignores a stale socket's close. UI adds a `reconnecting` state with a friendly countdown ("Hope you're having a lot of fun! Let's continue in {n}…") that dismisses the instant playback resumes.
- **Why:** replaces the old dead-end ("reopen to continue") so a ~30-min chapter survives the ~10-15 min socket limit.
- **Caveat:** preview-API field names (`sessionResumption`, `contextWindowCompression.slidingWindow`, `sessionResumptionUpdate`) confirmed working against `gemini-3.1-flash-live-preview`; may drift server-side (A3/A9).

### A12. Interruption robustness: ambient noise vs. second speaker (next)
- **Status:** Scoped, **not built**.
- **Two distinct problems:** (a) **ambient noise** (cafe) — *cheap*: mic already requests `echoCancellation`/`noiseSuppression`/`autoGainControl`; add VAD sensitivity/prefix-padding tuning + optional WASM denoise (RNNoise). (b) **second human speaker** (call-centre) — *hard*: needs **voiceprint enrollment + client-side speaker verification** (ONNX/WASM speaker embedding) gating audio *before* Gemini, since Gemini Live has **no server-side target-speaker hook**; likely forces the AudioWorklet migration (A9).
- **Key fact:** pitch alone is **not** a reliable identifier — a speaker *embedding* (timbre/formants, many dims) is. Do (a) first; (b) is a separate larger project.

### A13. Whole-book Gist (AI summary mode)
- **Status:** Shipped (client + edge fn) 2026-07-07 — **gated on Gemini quota/billing** (whole-book input is large).
- **Decision:** The "Book Gist (AI)" button opens the **same GeminiLiveModal in `gist` mode** — a reliable whole-book *summary* (not verbatim), conversational (interrupt + Q&A), with its **own admin-editable prompt** (`live_gist_prompt`, **Gemini-Live-only**; default fallback lives in `voice-session`). No verbatim word-alignment/progress in gist mode.
- **Client-side content:** the whole-book text is assembled in the browser (chapters already in `bookStore`) and sent to the edge fn — **no per-request DB blob fetch**, so concurrent gist requests don't amplify heavy reads.
- **Caveat:** whole-book context is a large input → hits Gemini quota on the current tier (needs billing/higher tier or Vertex; very long books may exceed context even with slidingWindow). RAG (A7) is the eventual fix.
- **Update (2026-07-07):** the BookDetail **Gist button was removed** so users don't hit the quota error; the feature code (edge fn `mode:'gist'`, modal gist mode, `live_gist_prompt`) is **retained**. Re-enable = action plan item 36.

### A14. AudioWorklet mic capture (the A9 fix, executed)
- **Status:** Shipped 2026-07-07.
- **Decision:** Mic capture moved **off the main thread** to an **AudioWorklet** (`src/lib/pcmCaptureProcessor.js`, batches ~2048 samples) with a **ScriptProcessorNode fallback** (try/catch on worklet load, so Talk always works). Emits `mic_capture {mode}` / `audioworklet_failed`.
- **Why:** executes the A9 fix ladder — the deprecated `ScriptProcessorNode` ran on the main thread competing with the waveform + React re-renders (choppy audio/laggy text after a few turns). Also the **prerequisite** for heavier audio work (RNNoise denoise, target-speaker — A12).

### A15. Durable session history + post-session star rating
- **Status:** Shipped 2026-07-07 (migrations `008` `voice_sessions`, `009` rating columns) — **backfilled into this log**, was already live but undocumented here.
- **Decision:** Every Talk/Gist session, across all three voice providers, gets one row in **`voice_sessions`**: `data` JSONB holds `{ meta, turns }` (the full transcript), written once at session end (`endVoiceSessionRecord`, `voiceSessionLog.js`). A mandatory 1-5★ + optional free-text rating (`SessionRatingScreen.jsx`) is collected right after, as a **separate follow-up write** (`submitSessionRating`) rather than bundled into the same call.
- **Why separate writes:** if the user abandons the tab on the rating screen, the transcript + `ended_at` (already saved) survive — only the rating itself would be missing. Matches the existing "best-effort, never block the voice UX" convention used for `voice_events`.
- **Distinct from:** `voice_events` (technical debug log: connects/drops/errors) and `voice_progress` (legacy narration progress bar, pipeline/Cartesia only — Gemini Live doesn't write to it).

### A16. Per-message thumbs up/down + confirmable remarks (Gemini Live only)
- **Status:** Shipped 2026-07-09.
- **Decision:** Each **agent** bubble in `GeminiLiveModal` gets thumbs up/down; thumbs-down opens a remarks field. `adminStore.setVoiceMessageFeedback` patches `{ thumbs, remarks }` directly onto the message object already held in `voiceTranscripts` — since `endVoiceSessionRecord` reads that same array as `turns` at session end (A15), feedback rides into `voice_sessions.data.turns[i].feedback` for free, **no new table or migration**.
- **Remarks require explicit submit:** typing only updates local component state; Enter or the send button commits to the store. Once submitted it collapses to a read-only line; clicking it re-opens editing. Chosen over live-as-you-type persistence — an editable field with no confirmation step reads as unfinished/uncertain input, not a submitted rating.
- **Timing caveat (inherited from A15):** feedback only survives if given before **End Session** is clicked — a crash/force-close mid-conversation loses it, same as the rest of the transcript.
- **Scope:** **Gemini Live only** — moot as a limitation now that it's the only Talk UI (`VoicePipelineModal`/`VoiceAgentModal` deleted, see C7); at the time this shipped it was a deliberate parking, not an oversight.

---

## B. Content pipeline

### B1. Content model: `books.chapters` JSONB with nested sections
- **Status:** Active. Each book row holds a `chapters` JSONB array; each chapter has `content` + nested `sections[]` (`{ number, title, text, cartesia_document_id }`). Section numbers **restart per chapter** — a bare section number is meaningless without its chapter.
- **Why:** Simple, colocated with the book; the voice agent reads sections straight from here. **Discipline:** never fetch sections via a flat query by `chapter_number` (numbers repeat) — always via the nested chapter object.

### B2. Admin content pipeline: EPUB → Generate → Split → Sync
- **Status:** Active. **EPUB** = extract chapter text (JSZip + DOMParser); **Generate** = Gemini authors chapter list / one-liners + section titles; **Split** = chunk `content` into ~350-word sections; **Sync KB** = push sections to Cartesia (folder + per-section docs). Gemini Live needs only EPUB + Split (Sync is Cartesia-only).

### B3. 100%-coverage chunking (merge-not-drop) + assertion
- **Status:** Fixed in code; **existing books still need a lossless re-split** (action item #2).
- **Why:** The old `splitIntoSections` **dropped every paragraph ≤40 chars**, silently losing text (measured: *The Hard Thing* ch18 kept only 38%). New splitter **merges** short paragraphs; `sectionCoverage()` asserts 100% and the admin flags `⚠️cov`. Re-splitting existing books must **preserve section titles + `cartesia_document_id`** (naive re-split wipes them).

### B4. EPUB parser is a text extractor, not a rich parser
- **Status:** **Superseded 2026-07-09 — see B7.** Extracts clean reading text + h1–h4 + basic lists. **Drops** images, link URLs, page numbers, tables, `<figcaption>` captions. DRM/non-ZIP files now fail with a clear message (PK signature check).
- **Why:** Optimized for narration text. Rich extraction (images/captions/links) is a future parser upgrade if needed.

### B5. Hard-delete book + Cartesia de-sync (parallel batches)
- **Status:** Shipped. Admin 🗑 → `book-delete` edge function: de-syncs Cartesia docs+folder, deletes `voice_progress`, deletes the book (sessions/requests/etc. cascade via FK).
- **Why batched:** Sequential deletion of 149–345 Cartesia docs took >60s → browser dropped the request. **Parallel batches of 25** → ~3–5s. Lesson: any per-item loop over hundreds of external API calls must be parallelized/batched.

### B6. OCR ingestion (Sarvam Vision) — parked / research
- **Status:** Parked (research). See action plan item 35.
- **Decision:** Consider **Sarvam Vision / Document Digitisation API** (3B VLM; English + 22 Indian languages; preserves structure/reading order; strong Indic OCR) as a **second ingestion path** for scanned / image-based PDFs the EPUB parser can't handle (B4). Server-side via an edge fn (key off the client); output feeds the existing **Generate → Split**.

### B7. Structured content model — blocks replace the flat content string
- **Status:** Shipped 2026-07-08/09 (parser + storage). **Phase 3 — live multimodal wiring in `geminiLive.js` so the agent actually uses this in conversation — not yet built.**
- **Decision:** `chapter.content` (flat string) is now **derived and cached** from a new `blocks` array per chapter — `heading` (real level 1-4, not a `#`-prefix hack), `paragraph` (inline `spans` with `bold`/`italic`/`underline` marks), `list`, `table` (real row/cell structure), `image` (alt/caption + uploaded asset URL — see B8/B9), and `svg` (inline vector charts, raw markup + title/desc). Blocks emitted from inside an `<aside>` or an element tagged with a recognized `epub:type` (`sidebar`/`tip`/`note`/`practice`/etc.) carry a `role` field — `<aside>` is **no longer stripped** (the old parser deleted it outright; in real EPUBs it holds genuine sidebar/tip content, not decoration). Page markers (`epub:type="pagebreak"`) captured best-effort — many EPUBs have none at all (page numbers are a print concept), useful where present for loss/coverage QA and conversational "as covered on page N" references.
- **Bold/italic/underline captured but NOT used in voice this phase** — confirmed Gemini Live's TTS has no SSML/per-word emphasis control, so "faithful emphasis in audio" isn't mechanically possible today; data is there if a future surface can use it.
- **No pre-baked AI captions for images/charts.** Confirmed Gemini Live's `realtimeInput` accepts image frames into an active session (base64, same channel as mic audio) — the agent will get the actual image as live multimodal context and describe it only if the conversation calls for it, not a canned description written ahead of time.
- **Folded in:** the multi-file-chapter-merge bug (an untitled spine file under ~500 words merges into the previous chapter instead of becoming a bogus new "Chapter N"; a substantial untitled file still gets its own chapter, to avoid silently merging two real chapters when a book's TOC is sparse).
- **Why no visual reader UI:** the product surface for this is conversational (speech bubbles), not a rendered document view — see D5's AI-only direction. A synced visual read-along panel was explicitly scoped **out** as a follow-on, not part of this work.

### B8. Blocks live in their own table, not nested in `books.chapters`
- **Status:** Shipped (migration `010_book_content_blocks.sql`).
- **Decision:** `book_content_blocks` — one row per (book, chapter) — holds `blocks`, **not** nested inside `books.chapters`.
- **Why:** blocks are far more verbose JSON than the old flat string (every paragraph is now a spans array; every image carries assetUrl/alt/caption/page). Nesting them would bloat **every** unrelated read/write of a book's chapters — including the admin Catalog tab's eager `select('id, chapters, ...')` for the **entire** catalog at once, and every Split/Generate/Regen write, which rewrite the whole `chapters` column regardless of what changed. Confirmed in practice: embedding blocks in `chapters` visibly slowed the admin Books tab; moving them out (and re-parsing) measurably fixed it. Anticipated to matter more, not less, as more image/table-dense titles are added.
- **Rejected:** stripping `blocks` from the admin listing via a Postgres JSONB-transform RPC — a band-aid on one query that leaves the underlying write-amplification problem (Split/Generate/Regen still shuttling a growing blob) unfixed.
- RLS: public read (same sensitivity as `books.chapters`, already public); admin-only write (reuses `is_admin()` from migration 007). Cascades on book delete via FK — no extra cleanup code needed.

### B9. Image/asset storage (`book-assets` bucket)
- **Status:** Shipped.
- **Decision:** Images extracted from the EPUB zip during parsing, uploaded to a **public Supabase Storage bucket** `book-assets` (one folder per `book_id`), URL stored on the image block (`src/lib/bookAssets.js`). Bucket created manually via the dashboard (Storage buckets aren't SQL migrations).
- **Real gotcha (migration `011_book_assets_storage_rls.sql`):** a Storage bucket's "public" toggle only controls **reads**. Uploads are governed separately by RLS on Supabase's internal `storage.objects` table, which denies everything until an explicit policy grants it — every image upload failed with `new row violates row-level security policy` until this policy was added (admin-only write via `is_admin()`, public read). Same mental model as table RLS, easy to miss since Storage doesn't surface it as obviously.
- Cleanup: `book-delete` edge fn purges a deleted book's asset folder (Storage isn't FK-linked, so this needs explicit code, unlike `book_content_blocks`'s cascade).

### B10. Real EPUB files are messier than the spec — lessons from a live debugging session
- **Status:** Fixed; worth remembering for any future ingestion work.
- **Found via a real book ("Romancing The Balance Sheet"):** (1) **a wrapping top-level folder** in the zip — the file had been re-zipped from an *extracted* folder rather than its contents (a common real-world mistake, e.g. macOS/Windows "Compress" on an unzipped folder), so `META-INF/container.xml` wasn't at the true zip root. Fixed by locating `container.xml` anywhere in the archive and treating whatever precedes it as a shared prefix applied to every subsequent path — a general, non-destructive fix, not a one-off hack for this file. (2) **HTML file inputs don't fire `onChange` when the exact same file is re-selected** (a known browser quirk) — silently made repeated debugging attempts look like "nothing happens." Fixed by resetting the input's `value` before each pick. (3) **Errors caught by the UI were being silently swallowed** — only shown in a 50-character-truncated status badge, never logged to console — made every one of the above take far longer to diagnose than necessary. Fixed by adding explicit `console.error` at the catch site.
- **Lesson:** when a parser/ingestion error is opaque, harden the failure path (clear error messages + full console logging) **before** trying to guess the root cause from a truncated UI string — this alone would have cut the diagnosis time substantially.
- **Why not now:** EPUB covers the current catalog; OCR is only for scanned/image sources; free through Feb 2026 but **post-free per-page pricing** (a one-time ingest cost), async/batch for long books, and copyright of scanned commercial books all need checking. Preview API.

---

## C. Data / infra

### C1. `bookStore` eager-loads all content (3 MB) — lazy-load agreed
- **Status:** **Shipped (2026-07-06).**
- **Why:** Public `bookStore.fetchBooks()` did `select('*')` → downloaded **every book's full chapter text (~2.77 MB of 3 MB)** on startup just to show the grid; grew linearly with the catalog → slow.
- **Implementation:** `fetchBooks()` now selects **light columns only** (no `chapters`); new `fetchBookChapters(bookId)` (memoized) lazy-loads a book's `chapters` when its `BookDetail` opens (spinner while loading), so Talk inherits them. `adminDataStore` was already light. Full-text-only-at-Talk still deferred (single-JSONB storage would need server work); the current per-book load on BookDetail-open is the accepted cost.

### C2. `language` column added; `publisher`/`pub_date` dropped
- **Status:** Shipped. `addBook` was inserting 3 non-existent columns → PostgREST error. Added `language` (migration 004, useful for multilingual roadmap); removed `publisher`/`pub_date` from insert + their dead display lines.

### C3. Repo layout + remote
- **Status:** Active. The project lives at **`hridyanshu7/books-p2p/vyaz/`** (nested one level — a Claude session is anchored to `books-p2p/`, so we keep the project inside it; the `vyaz/` name was the intended rename). GitHub remote = **`Hridyanshu7/vyaz-web.git`**. Deploy: `vercel --prod` (or git push) for frontend; `npx supabase functions deploy <name>` for edge functions.

### C4. `voice_provider` public scoped read (migration 006)
- **Status:** Shipped 2026-07-06. **Superseded 2026-07-12 — see C7.** `bookStore.fetchVoiceProvider()` and the whole precedence chain this enabled were removed; `GeminiLiveModal` is now unconditional. The migration 006 RLS policy itself was left in place (harmless, unread) — not worth a new migration just to drop it.
- **Decision:** `platform_settings` is admin-only (holds secrets), so non-admin/logged-out users couldn't read `voice_provider` → Talk fell back to the wrong provider (Cartesia). Added a **scoped RLS policy** exposing ONLY the `voice_provider` row to `anon`/`authenticated` (each setting is its own row → secrets stay hidden). Client `bookStore.fetchVoiceProvider()` reads it; `BookDetail` precedence = admin settings → public read → `gemini_live` default.
- **Why:** a public page's behaviour must not depend on admin-only data, and secrets must not leak (RLS is row-scoped, so a single key can be exposed safely).

### C5. `profiles` RLS lock + `is_admin()` helper (migration 007)
- **Status:** Shipped 2026-07-07.
- **Decision:** A leftover `"Public profiles" (SELECT true)` policy (from the removed public narrator directory) let **anon read every profile incl. email** — a PII leak. Dropped it; profiles now readable only by the owner (`auth.uid() = id`) or admins. Admin checks use a **`SECURITY DEFINER public.is_admin()`** helper.
- **Lesson:** an inline `EXISTS(select … from profiles …)` inside a policy **on** `profiles` causes **42P17 infinite recursion** once the permissive public policy is gone — a policy on a table that queries the same table needs a SECURITY DEFINER helper.

### C6. Admin moved to its own `/admin` route
- **Status:** Shipped 2026-07-07.
- **Decision:** The Admin panel used to be a **tab inside the Dashboard**; removing `/dashboard` in the P2P pivot orphaned it. Gave Admin its own **`/admin` route** (`is_admin`-gated, non-admins → `/`) + an admin-only header link. Regression fix surfaced by the pivot.

### C7. Pipeline + Cartesia voice-agent modals removed — Gemini Live is the only Talk UI
- **Status:** Shipped 2026-07-12.
- **Decision:** `VoicePipelineModal.jsx` + `VoiceAgentModal.jsx` and their sole client dependencies — `src/lib/voicePipeline.js` (discrete STT→LLM→TTS pipeline logic) and `src/lib/voiceAgent.js` (raw-WebSocket Cartesia agent client) — are **deleted**, not just unused. `BookDetail` renders `GeminiLiveModal` unconditionally — no more `voiceProvider` branching, no more `bookStore.fetchVoiceProvider()`/`voiceProvider` state (both removed, see C4). Admin → Agents dropped `ProviderSwitcher` and `PipelineModelsCard` (and the `pipeline_*`/`voice_narration_prompt`/`voice_answering_prompt` settings they edited — now orphaned-but-harmless rows in `platform_settings`, nothing reads them client-side anymore).
- **What's deliberately still there:** the **Cartesia content-sync pipeline** (admin "Sync" button, `cartesia_folder_id`, `cartesia-kb-sync`/`book-delete` edge functions) is a *different* feature — pushes book text to a Cartesia knowledge base, unrelated to which UI plays a Talk session — and was explicitly kept. `CartesiaCard` in Admin → Agents was trimmed (dropped `cartesia_voice_id`/`voice_agent_system_prompt`, the voice-agent-only fields) rather than deleted, since `cartesia_api_key`/`cartesia_agent_id` are still load-bearing for that pipeline; relabeled "Cartesia (Content Sync)" so it reads correctly now that it isn't a voice-agent option.
- **What's deliberately left alone:** the deployed `voice-session` edge function still has a dead `pipeline` branch (sttModel/llmModel/ttsModel/ttsVoice config) — harmless since nothing calls it anymore, and editing+redeploying live edge function code was explicitly out of scope for this cleanup. The `cartesia-token` edge function (minted sessions for the now-deleted `VoiceAgentModal`) is similarly orphaned-but-deployed, left alone for the same reason. Migration 006's public RLS read of `voice_provider` (C4) was also left in place rather than dropped via a new migration.
- **Why:** the product direction has been Gemini Live only for a while (see CLAUDE.md "Current direction"); Pipeline and Cartesia's voice-agent modals were dead weight once nothing routed to them by default. Removing the code (not just defaulting away from it) matches "we only use the Gemini Live modal design" rather than leaving two more unmaintained UI surfaces to bit-rot.

### C8. Mobile: Talk modal clipping fixed + landscape required below `sm:`
- **Status:** Shipped 2026-07-12.
- **Root cause (confirmed via code trace, not guessed):** `GeminiLiveModal`'s card was `overflow-hidden flex flex-col max-h-[90vh]`; below the `sm:` (640px) breakpoint its two-column body stacks vertically, and the controls column (`shrink-0`, holding the full per-chapter section list) had no height bound — it always rendered at full natural content height. For an *average* chapter (~13 sections, per `docs/unit-economics.md`), that content exceeds what's left after the header, and since the card only clips rather than scrolls, the excess — including the End Session button — silently vanished with no way to reach it. Git history showed commit `8e33c2f` had removed a prior fixed `max-h-28` safety cap on the section list when it introduced the current layout, with no mobile testing evidenced anywhere.
- **Fix:** `max-h-[90vh]` → `max-h-[90dvh]` (tracks the actual visible viewport on mobile browsers with dynamic chrome, not the address-bar-collapsed maximum) on both `GeminiLiveModal.jsx` and `VoicePipelineModal.jsx` (identical latent bug, before its removal in C7). Controls column got `max-h-[46dvh] sm:max-h-none` so its already-correct internal `overflow-y-auto`/`shrink-0`/`mt-auto` structure finally engages on mobile exactly as it already did on desktop — no internal changes needed, only bounding the ancestor. Verified via a throwaway harness (mock chapter, 15 sections, bypassing auth) across 3 mobile sizes + 1 desktop size: End Session always in-bounds, section list's `scrollHeight > clientHeight` (confirms the scroll actually engages), desktop unaffected.
- **Landscape requirement (same session, deliberate, not a fallback):** on narrow viewports, `GeminiLiveModal` now **requires landscape** — a `useOrientationGate()` hook (`matchMedia('(orientation: portrait)')` + `innerWidth < 640`, live-updating via `change`/`resize` listeners) shows a `RotatePrompt` instead of the modal body, and — critically — also gates the `connect()` effect itself, so **no Gemini Live session is opened** until the user rotates (avoids spending one of the ~3 concurrent session slots on a screen the user hasn't engaged with yet). Chosen deliberately over a softer dismissible hint (user's call, after that alternative was proposed) — rotating to landscape (~660-930px wide on a phone) crosses the same 640px breakpoint that unlocks the two-column layout, so this is framed as "unlock the richer layout that already exists," not new UI. Scoped to `GeminiLiveModal` only — `VoicePipelineModal` was single-column regardless of orientation (moot now it's deleted, C7) and `VoiceAgentModal` never got the same treatment before its removal.
- **Smaller mobile-audit fixes, same session:** `BookSearch.jsx`'s genre-pills row (`overflow-x-auto`) had no visual affordance that it scrolled — the last pill clipped mid-word at the screen edge; fixed with scroll-position-tracked fade gradients (`canScrollLeft`/`canScrollRight`, updated on scroll+resize) rather than a static always-on fade. `BookDetail.jsx`'s per-chapter "Talk" button used the shared `Button size="sm"` (~34px tall, under the ~44px recommended touch target); fixed with a scoped `min-h-11` on that one instance rather than changing the shared `Button.jsx` `sm` preset used broadly elsewhere.
- **Context:** this session also decided **no native Android/iOS app** — estimated 6-12 weeks MVP / 3-4 months full parity for a native rebuild (Gemini Live's full-duplex WebSocket + AudioWorklet mic pipeline has no native equivalent, would be the dominant cost), chose to stay web-only and invest in mobile-responsive fixes instead.

### C9. Sign-in requirement removed — Browse, Talk, and Profile all work signed-out
- **Status:** Shipped 2026-07-12 (commits `3a4b6f8`, `6fe1693`).
- **Decision:** `SignupModal.jsx` and its `useSignupModal` store are **deleted**, not just unused. The header's "Sign in" button (desktop + mobile) is gone. Every auth gate that used to open that modal now proceeds without it: `Layout.jsx`'s post-signup `onboarding_complete` nudge is gone; Home's "Get started" button goes straight to `/books` regardless of auth state; `BookDetail`'s Talk button no longer checks `user` before opening `GeminiLiveModal` (see C7/C8 for the modal itself); `Profile.jsx` no longer force-redirects an unauthenticated visitor into a sign-in modal — it just renders with empty fields. A follow-up commit (`6fe1693`) fixed a leftover `useSearchParams()` call the first commit had made dangling (the `?talkChapter=` post-signup deep-link resume effect it belonged to was removed as part of the same cleanup, since there's no more post-signup redirect to resume from).
- **What's unchanged:** `/login` (Google/LinkedIn OAuth + email magic-link) still exists and works exactly as before for anyone who wants an account — this removes the *requirement*, not the *option*. Admin (`/admin`) is still `is_admin`-gated as before (C6) — this change doesn't touch admin auth.
- **Why:** removes signup friction from the core Talk experience — reading/talking to a book now needs zero commitment before trying it, matching the product's "skip the reading, just talk to it" pitch. Accounts remain available for anyone who wants Profile persistence or a saved history.

### C10. `notify-signup` — admin email alert on new signups
- **Status:** Shipped 2026-07-12 (uncommitted at time of writing — `supabase/functions/notify-signup/`).
- **Decision:** New edge function triggered by a **Database Webhook on `auth.users` INSERT** (deliberately not `profiles` INSERT — at the instant of signup, `handle_new_user()` only writes `{id, phone}` to `profiles`; `auth.users` always has the real email + OAuth name/avatar in `raw_user_meta_data`, so hooking the earlier table gets complete data in one shot). Sends via **Resend**, config through `platform_settings.resend_api_key` / `notify_signup_email` — no-ops rather than erroring if unconfigured, so an un-set-up webhook doesn't spam retries.
- **Why now:** with C9 removing every sign-in gate, an account is no longer a forced checkpoint in the user journey — this gives the team visibility into signups happening organically instead of at a known, instrumented moment.

---

## D. Strategy / business

### D1. Target segment: premium learners (India)
- **Status:** Active positioning. Students/aspirants/professionals/English-first; WTP ~₹299–499/mo. Positioned as a **premium AI study companion**, priced to stay India-accessible.
- **Why:** Mass value-conscious Indian consumers cap at ₹99–199/mo — **below our per-chapter cost**. Premium learners' WTP actually clears cost.

### D2. Pricing: Freemium 3-tier + verbatim credits
- **Status:** Recommended (see [pricing.md](pricing.md)). Free (2–3 concise ch) → ₹199 Learner → ₹449 Scholar; verbatim as ₹99/ch credits. **No text-summary tier** — the transcript *is* part of the audio experience, not a separate SKU.
- **Cost lever:** concise/discussion mode (~₹17/ch) as everyday default vs verbatim (~₹75/ch) premium. Cost reduction (₹17→₹10) is strategic, not cosmetic.
- **Update (2026-07-07): concise mode decided against.** No non-verbatim narration mode — reaffirms A4 (concise/discussion would also re-break progress tracking + black/grey classification, which require verbatim delivery). Pricing must lean on other levers (credits, tiering), not a concise SKU.

### D3. Unit economics + the scaling wall
- **Status:** Documented ([unit-economics.md](unit-economics.md)). ~$0.90 per verbatim chapter (~30 min audio; audio-output dominates). Two hard limits: **~3 concurrent Live sessions/key** (→ Vertex for 1,000) and **~10–15 min per WebSocket connection** (a 30-min chapter needs session resumption — not yet built).

### D4. Voice-provider comparison conclusion
- **Status:** Documented ([voice-providers-comparison.md](voice-providers-comparison.md)). **Gemini Live stays default** (cheapest all-in, unified, multilingual). **Bolna** is the only one worth revisiting — and only for a **phone-based India channel** (telephony, not language; Gemini already does Indian languages). Vobiz = telephony infra, not a voice-agent brain.

### D5. Pivot to AI-only — remove human-narrator / P2P
- **Status:** Active — **Phase A shipped** (P2P UI hidden); Phases B (dead-code delete), C (DB drops), D (docs) in progress. See action plan items 24–28.
- **Decision:** Remove the entire human-narrator / P2P side — narrator profiles, availability, bookings, reviews, group sessions, requests, and the Google Calendar (`gcal`) integration. **Humans are only users; all narration is AI** — verbatim chapter Talk + whole-book **Gist** (A13). Narrator `role`/columns deleted outright (never returning); **no data export** (only 2 users, both narrator+listener).
- **Why:** focus the product on the AI-voice thesis (faithful narration, anti-slop, conversation as the human medium) instead of an unused two-sided marketplace. This **supersedes the "human narrator sessions" framing** throughout the older docs.
- **Rollout discipline:** phased, **code-first / DB-last** — dropping a table while code still queries it 404s prod and can crash the catalog/admin `Promise.all` inits.

---

## E. Design & brand

### E1. Design language v2 — ink/paper/seal replaced with tinted-canvas/white-card/indigo-accent
- **Status:** Doc rewrite shipped 2026-07-14 (`docs/design-language.html`). Frontend implementation is a separate, staged follow-up — not yet started as of this entry.
- **Decision:** Scrapped v1's black-ink/white-paper/single-red-seal visual language (§5–§11 of the doc) in favor of a new system: a subtly tinted page canvas (`#F6F6FA`) with pure-white raised cards, a new primary electric-indigo accent (`#5B4EF4`) used liberally as the default interactive color (reversing v1's "spend the seal like it costs money" rule), a real (restrained) shadow/elevation system, a collapsed radius scale, and **Geist**/**Geist Mono** replacing Inter as the typeface (dropping the never-actually-shipped IBM Plex Serif idea entirely). Star ratings get their own dedicated amber token (`--color-rating`), separate from the brand accent. The Vyasa seal mark (व glyph, red `#E0201A`) is **kept but demoted** — confined to the logo and the Talk "Live" indicator dot only, not the app's dominant accent. Dark mode is explicitly out of scope for this pass (tokens are plain CSS custom properties so it can be added additively later, not a rewrite).
- **Why:** the v1 doc was never actually implemented in the app — `src/index.css`'s Tailwind theme had quietly drifted to generic Tailwind defaults (stock red `#DC2626`, Inter-only, a flat single-tone gray) — so the live product read as generic/dated rather than premium. Direct user feedback: the result looked closer to older Indian reading platforms (Pratilipi, Hubhawks) than to the modern SaaS/AI products (Linear, Notion, Perplexity, ElevenLabs) the target audience — Indian professionals fluent in LinkedIn → YouTube → Instagram → WhatsApp, in that order — actually associates with quality and trust.
- **What's unchanged:** the doc's core thesis, personality/voice, and positioning (§1–§4, lightly edited only) — author-fidelity and anti-slop were never the problem; only the visual execution was.
- **Codebase implication for the follow-up frontend pass:** color-token discipline app-wide is excellent (virtually every color in every page/component routes through the named tokens in `src/index.css`), so redefining token *values* while keeping token *names* stable should cascade almost everywhere with minimal per-file edits. Radius (6 different scales in use today) and shadows (near-unused) are the two things that need real per-file cleanup, not just a token swap. `GeminiLiveModal.jsx`'s state-driven color meanings (waveform speaking/listening/idle, verbatim-vs-aside transcript styling, section-progress states) must keep their *distinctions* through any reskin, not just get recolored blindly.

### E2. Signature gradient (`--gradient-signal`) — one decorative brand moment, rationed to 4 spots
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1. Not yet implemented in the app.
- **Decision:** Added one decorative gradient token, `--gradient-signal: linear-gradient(135deg, #4A3ECB 0%, #1F9EA8 55%, #F5A623 100%)` (indigo → teal → gold), used in exactly four places: the व logo mark's fill, one ambient hero-glow behind Home's headline/CTA, an ambient blurred glow behind the book cover during an active Talk session, and — added after a follow-up round of feedback — the single Talk/voice CTA button itself (new `.btn-signature`), at most one per screen. Every other primary button (`.btn-primary`: Save, Continue, Get started, etc.) stays flat indigo; filled buttons now also carry a resting `shadow-raised` (not just on hover) so they read as tactile rather than pasted-flat. The live/recording indicator dot stays **flat** seal red (`--color-seal`) — gradients never carry semantic/status meaning, only flat tokens do (the Talk-CTA exception is deliberate: that button is functionally the same category as the logo, not an ordinary semantic control). No colored ring or stroke is drawn around any thumbnail/avatar to signal "active" or "live" — glow-behind, never ring-around.
- **Why:** direct user feedback after reviewing the E1 doc — the new palette read as premium but flat/personality-light. The ask was for some of the fluid, expressive color energy of apps like Instagram, with an explicit requirement that the result must **not read as inspired by Instagram** — so the gradient's color ramp was deliberately built to avoid every hallmark of that specific gradient (no magenta/pink anywhere in the ramp, no orange finish, includes teal — a hue that kind of gradient never uses, lands on gold instead) and the ring-around-thumbnail affordance was rejected outright as a UI pattern, independent of color, since the pattern itself reads as an homage regardless of hue. A second follow-up round of feedback on the button demo specifically ("buttons still lack personality") led to reviving v1's old "the seal button is reserved for the single most brand-defining action" instinct — v2 had dropped that idea entirely when it demoted the seal; the fix was to give it back, expressed through the gradient on the Talk CTA specifically rather than flat seal-red.
- **Why rationed to 4 spots, not a UI-wide treatment:** reference products (Linear, Notion, Perplexity, and — for what it's worth — Instagram's own actual product chrome) all keep gradients to a single brand/logo/hero moment and otherwise stay flat for legibility, accessibility (gradient text/buttons have unpredictable contrast), and to preserve the "clean modern SaaS" read established in E1. The Talk CTA is the one interactive exception because it's a singular, at-most-once-per-screen element — not a repeated component type like "primary button" or "badge" — so it can't create the visual noise a gradient-on-every-primary-button approach would.

### E3. Talk CTA becomes icon-only, no visible label — iterated from play-in-bubble to a mic glyph
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1/E2, iterated twice more same day. Not yet implemented in the app (`BookDetail.jsx`'s Talk button, `Home.jsx`'s hero CTA, etc. still show text as of this entry).
- **Decision (current):** The signature Talk/voice button (E2) carries no visible copy at all — no "Talk to this book," no "Play" — just a bold white **mic glyph** (capsule + stand-arc + stem + base, Lucide-`Mic`-style) on the `--gradient-signal` fill, in a `--r-pill` circular button (`.btn-signature-icon`, 60px button / 36px icon). Real accessible name preserved via `aria-label`/visually-hidden text: `"Talk to this book"`, not `"Play"` or `"Mic"` — screen-reader users still get the conversational framing even with zero visible words. Underlying interaction unchanged: verbatim narration + interrupt-to-ask-questions.
- **Iteration history (why it changed twice):**
  1. First pass: user wanted a "Play" concept, icon-only, "only visual elements that deliver message more effectively." Flagged that a bare play triangle reads as *media/podcast playback*, undermining the conversational promise. Proposed 3 concepts (play-in-bubble / waveform glyph / abstract facing-arrows); user picked **play-in-bubble** (a play triangle nested in a speech-bubble outline) to keep the triangle's "tap to begin" legibility while the bubble reframed it as dialogue.
  2. Icon-size bug: the rendered icon was too small relative to the 60px button (26px icon, non-square 28×24 viewBox with internal padding compounding the smallness) — fixed by enlarging to 36px and redrawing on a tight square 24×24 viewBox.
  3. On seeing it rendered, user judged play-in-bubble "doesn't look like a good CTA for launching a talk session" — it read closer to a "watch a video message" icon than "start talking." Root cause: a play triangle is inescapably media/broadcast-coded regardless of container shape. Proposed 3 replacement concepts (mic+soundwave-arcs / mic-in-bubble / waveform-only); user's own criterion was "something very familiar as clickable, aligns with the action" rather than picking literally from the list, so the mic glyph alone (no bubble, no arcs) was chosen as the synthesis — it's the single most universally recognized "tap to start talking" convention across voice apps (generic UI vocabulary, not an homage to any one product, unlike a specific gradient recipe or a ring-around-a-thumbnail would be), and non-arbitrary: `GeminiLiveModal.jsx` already shows this exact mic glyph pulsing during its "listening" state, so the launch button now previews a state the product already has rather than inventing a new symbol.
- **Follow-up not yet decided:** the exact SVG in the doc is a first-pass mockup (simple shapes, not a finished icon asset) — treat it as a spec of the *concept* (mic glyph, white-on-gradient, circular, no bubble) rather than pixel-final when this gets built into the real app; likely just reuse `lucide-react`'s actual `Mic` icon component for consistency with the rest of the codebase's iconography.

### E4. The Session Modal gets its own doc section (§12), inserted before Accessibility
- **Status:** Superseded same-day by **E5** — kept for history, do not build against this entry.
- **Decision:** Added a full new §12 "The Session Modal" covering every state of `GeminiLiveModal.jsx` under the new v2 tokens, assuming its *current production* two-column structure (transcript-with-verbatim-narration left ~70%, waveform+per-chapter-section-progress+End-Session right ~30%) carried over unchanged. This turned out to be the wrong structural assumption — see E5.
- **Why it was superseded:** after seeing the first mockup rendered, the user flagged it as "not in accordance with the new structure as per block-lab" — a separate prototyping sandbox (`books-p2p/block-lab/src/components/talk/`) had already explored a materially different information architecture that this entry didn't account for.

### E5. Session Modal restructured to match the block-lab redesign: conversation-only chat + live book-highlight stage + whole-book seek
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1–E4. Supersedes E4. Not yet implemented in either app — `GeminiLiveModal.jsx` (production) and `block-lab/src/components/talk/*` (prototype) both still need to be brought in line with this doc; this is the settled spec to build toward, confirmed as canonical by the user, not a rough sketch.
- **Decision:** Rewrote §12 around the structure found in `block-lab/src/components/talk/TalkLayout.jsx`, `ConversationPanel.jsx`, `BookStage.jsx`, `BookSeekBar.jsx`, `LiveTransportBar.jsx`, combined with production features block-lab hadn't ported yet:
  - **Verbatim narration moves off the chat entirely.** It now lives on the book's own real, fully-formatted page (headings/paragraphs/lists/callouts/tables/images from the structured `blocks` content model — CLAUDE.md/DECISIONS B7–B10), word-highlighted "karaoke"-style as it's read: covered text full opacity, the active block gets an accent left-border + tint and per-word reveal, not-yet-read text and blocks sit at reduced opacity (35–40%).
  - **The Conversation panel becomes conversation-only** — it shows just the agent's own asides (italic, ink-soft, thin left border, no bubble) and the user's spoken turns (a proper canvas-tinted bubble) — no verbatim narration bubble exists anymore.
  - **The waveform + state label move from a right sidebar to a top transport bar**, spanning the whole modal width, alongside the End Session button (ported from `LiveTransportBar.jsx`'s pattern, minus its Simulated/Live sub-mode toggle — confirmed block-lab-only prototyping scaffolding, not real product UI).
  - **The per-chapter section-progress list is replaced by a whole-book seek bar** (ported from `BookSeekBar.jsx`): one continuous track for the entire book, a dot per chapter sized by cumulative word count, click-to-jump-to-chapter-start (no mid-chapter scrub — live Gemini audio can't jump mid-stream).
  - **Mobile is a Chat/Book tab switch**, not a vertical stack, with the seek bar staying visible under either tab.
  - **Carried over from production, unchanged apart from re-theming:** per-message thumbs up/down (now attached to asides/Q&A answers specifically, since that's the only agent content left in the chat), book/chapter header + Live indicator, the reconnect overlay, the mobile rotate-gate (session still doesn't open pre-rotation), and the mandatory end-of-session rating screen.
- **Why:** the user had already prototyped and validated a better structure in block-lab before this conversation, and the first Session Modal pass (E4) was built from the current production modal alone without knowledge of it. Once surfaced, block-lab's structure was confirmed canonical rather than directional — moving verbatim text onto the book's own page (instead of into a chat bubble) is judged a **stronger** expression of the anti-slop thesis than the design in E4, not just a different layout: there's no ambiguity left about whether the highlighted words are the author's, because they're literally rendered as the author's real page.
- **Open follow-up:** neither codebase actually matches this spec yet. Production's `GeminiLiveModal.jsx` still has the old transcript-with-verbatim-narration + sidebar-progress-list structure; block-lab's prototype has the new layout but not yet the carried-over production features (thumbs feedback, rating screen, rotate-gate, full header). Building either toward this doc is separate, not-yet-scoped work.

### E6. Live tag + End Session button + waveform strip collapsed into one "voice orb" control
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1–E5. Supersedes E5's header/transport-bar treatment specifically (the two-panel body, whole-book seek, and carried-over production features from E5 are unaffected). Not yet implemented in either codebase.
- **Decision:** Removed the separate Live tag (`.tag-seal`/`.live-dot`, both now dead CSS, deleted) and the header's "End session" button entirely. In their place: one circular control, the **voice orb** (`.voice-orb`), centered below the header — waveform bars render inside it, and the orb's own fill color is the primary state signal (accent = agent speaking, sage = listening/user's turn, neutral-bordered with a static mic glyph while idle/connecting — same mic glyph as the launch CTA, E3, for continuity). The orb is the single control surface in the modal: once live, ending the session, jumping to a chapter, or asking a question are all just spoken to it, handled by the agent, with no dedicated button for any of them. A small text state label stays underneath for accessibility (color can't be the only signal, §13) but carries no interactive function of its own.
- **Why:** direct user feedback that a discrete Live tag + End Session button + waveform bar was three separate things competing for attention where one would do, and that ending a session or changing chapters shouldn't need dedicated UI at all when the whole point of the product is that you can just talk to it. Reframes the modal's chrome around a single principle: if it can be a spoken command handled by the agent, it doesn't need to be a button.
- **Consequence for §3/§5 (the seal):** flat `--color-seal` loses its only remaining active use (the Live-status dot) — the mark itself already uses the signature gradient (E2), not flat seal red, so as of this entry **`--color-seal` has no active UI use at all**. Kept defined and documented as reserved for a plausible future use (e.g. an author-verification badge) rather than forced into a role it no longer needs, consistent with how dark mode (E1) is handled — deferred, not deleted.
- **Consequence for whole-book seek:** its click-to-jump-a-chapter interaction stays as a direct-manipulation convenience alongside the orb's voice command — the two aren't in conflict, just two paths to the same result.

### E7. Voice orb shrunk from a full-width hero band to a small floating corner control
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1–E6. Not yet implemented in either codebase.
- **Decision:** E6's orb was a 76px circle centered in its own full-width row directly under the header — on review this read as a wasteful hero-banner treatment (a lot of empty canvas-colored space either side of one small circle, and a large chunk of the modal's vertical space spent before reaching any actual content). Reworked: the orb shrinks to 52px and becomes an absolutely-positioned floating control (`.orb-float`) anchored to the bottom-right corner of the modal, overlapping the content slightly — the same convention as a call app's control cluster or a chat widget's compose button — rather than claiming a dedicated layout row. Its small state-label (previously a full text row underneath) becomes a compact pill sitting beside it in the same floating cluster. The header goes back to being just the book/chapter title, full stop — no chrome of any kind competes with it now.
- **Why:** direct user pushback on the E6 rendering — "why does it have to be so big, why does it have to be at the top" — a fair read given the actual screenshot showed exactly that imbalance. A persistent always-on utility control should be reachable, not dominant; floating it in a corner is the established pattern for exactly this kind of control across chat/call UIs, and reclaims the vertical space for the header and the two-panel content underneath.
- **Mobile, addressed explicitly:** added a dedicated narrow (~340px) mockup showing the orb floating in the same fixed corner regardless of which Chat/Book tab is active or how narrow the viewport gets — it does not resize, move, or get a mobile-specific treatment; only the panel underneath it swaps. This was a direct, explicit gap in E5/E6 (mobile was asserted to "just work" via the existing tab-switch media query, never actually shown) — now shown, not just claimed.

### E8. Reading panes made taller; mobile mockup made truly full-screen with both tab states shown
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1–E7. Not yet implemented in either codebase.
- **Decision:** Three fixes from direct follow-up feedback on E7's mockups: (1) `.talk-body`'s `min-height` raised 260px → 440px, and the Book panel's demo content extended from 3 short lines to a fuller passage (opening line, two read paragraphs, the active paragraph, two unread paragraphs) so the taller pane actually reads as a populated reading surface, not empty canvas around a peeking sliver of text. (2) New `.phone-mock` treatment (fixed `300×620px`, `display:flex; flex-direction:column`, header/tabs/seek-bar `flex:none`, content area `flex:1` scrollable) replaces the previous mobile mockup, which used `min-height` on the content block and could visually read as a small floating card with slack space rather than a screen-filling view — the flex approach forces the content region to actually stretch to fill all available vertical space between the fixed chrome, the way a real mobile viewport does. (3) Both Chat-tab-active and Book-tab-active states are now shown side by side (previously only Chat was ever rendered) — the Book tab's mockup reuses the same karaoke-highlight demo content as the desktop version, captioned separately.
- **Why:** three separate but related "this doesn't look/feel right" callouts on the same rendered screenshot — a cramped two-line reading pane doesn't demonstrate that the Book panel is meant to be the primary reading surface (the whole point of E5's restructure); a mobile mockup sized like a floating card rather than a real phone screen undersells that the session goes full-screen on mobile; and only ever showing one of two tab states left the second, equally real state undocumented. None of these were deliberate design choices in E7 — they were mockup-fidelity shortcuts that ended up misrepresenting the actual intended design.

### E9. Voice orb's visible "Narrator speaking" pill removed — state moves to a screen-reader-only announcement
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1–E8. Not yet implemented in either codebase.
- **Decision:** Dropped the `.orb-label` pill that sat beside the orb showing its current state as visible text ("Narrator speaking" / "Listening" / etc., introduced in E6). The orb's fill color, icon (mic when idle), and waveform animation already carry this — the text was redundant. Now implemented as `<span class="sr-only" aria-live="polite">` inside the orb button: nothing visible changes on screen, but the state is still announced to assistive tech, preserving the accessibility principle from E7/§13 (color can't be the sole signal) without the on-screen clutter. `.orb-label` CSS (now fully unused) deleted.
- **Why:** direct feedback that the label was unnecessary next to a control that's already self-explanatory via color + icon + motion. A small, correct fix rather than a redesign — same pattern as earlier dead-CSS cleanups in this session (`.tag-seal`/`.live-dot`/`.session-close`/`.live-mark`, all E6): remove what's visibly redundant, keep the accessibility guarantee intact via a non-visual mechanism instead of dropping it outright.

### E10. Mobile landscape requirement dropped — portrait now connects immediately, same as desktop
- **Status:** Doc updated 2026-07-14 (`docs/design-language.html`), same session as E1–E9. Not yet implemented in either codebase — production's `GeminiLiveModal.jsx` still has the real C8 landscape gate live; this is the target to build toward, superseding C8's requirement specifically (C8's mobile-clipping-fix half is untouched and still correct).
- **Decision:** Removed §12's "Rotate to continue (mobile)" state entirely. A session now opens and connects in portrait exactly as it does on desktop — no rotate prompt, no gating the live connection on device orientation. Reverses the landscape-only rule from production DECISIONS **C8** for the *redesigned* modal specifically.
- **Why:** C8's landscape requirement existed for one reason — the old two-column layout (transcript/controls sidebar) needed desktop-like width to not feel cramped, so portrait was blocked until rotating crossed the `sm:` breakpoint that unlocked that layout. E5's restructure replaced that two-column layout with a Chat/Book tab switch built specifically to give one panel the *full* screen width on narrow viewports — the entire premise the landscape gate was protecting (this layout doesn't work in portrait) no longer applies once that tab layout is real. Caught by the user asking directly "what about landscape mode" after the rest of §12 had already changed; carrying C8 forward unexamined (as E5 originally did, "that gate is unchanged") turned out to be carrying forward a rule whose reason had already been designed away.
- **What doesn't change:** C8's *other* fix — the `max-h-[90dvh]`/mobile-clipping correction, and the underlying mobile-web-not-native strategy — is unrelated to the landscape gate and stays exactly as shipped. Only the orientation requirement itself is reversed.
- **Lesson for this whole redesign exercise:** when a later change removes the reason behind an earlier constraint, the constraint needs to be re-examined, not carried forward by default just because it wasn't the thing being explicitly discussed at the time.
